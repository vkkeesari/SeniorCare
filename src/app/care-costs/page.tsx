'use client';
import { useState } from 'react';
import { ArrowRight, Check, DollarSign } from 'lucide-react';
const costs = [{ name:'Independent living', min:2500, max:5000 },{ name:'Assisted living', min:4500, max:6500 },{ name:'Memory care', min:5500, max:8500 },{ name:'Nursing home', min:8000, max:11500 }];
export default function CareCosts() { const [type, setType] = useState(1); const [budget, setBudget] = useState(5500); const item = costs[type]; return <><section className="bg-ink text-white"><div className="container-shell py-20 md:py-28"><p className="eyebrow text-[#b7d0bd]">Care costs hub</p><h1 className="mt-4 max-w-3xl font-serif text-6xl leading-none">Clarity feels better than guessing.</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-300">Explore typical monthly ranges for the Triangle, then talk with Gabe about the details that make a real plan possible.</p></div></section><section className="container-shell grid gap-8 py-20 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-3xl border border-slate-200 bg-white p-7 shadow-soft md:p-10"><p className="eyebrow">Monthly planning tool</p><h2 className="mt-3 font-serif text-4xl">What kind of care are you considering?</h2><div className="mt-8 grid gap-3 sm:grid-cols-2">{costs.map((cost, index) => <button key={cost.name} onClick={() => setType(index)} className={`rounded-xl border p-4 text-left text-sm font-bold ${type === index ? 'border-moss bg-sage' : 'border-slate-200'}`}>{cost.name}<span className="mt-1 block font-normal text-slate-500">${cost.min.toLocaleString()}–${cost.max.toLocaleString()}</span></button>)}</div><div className="mt-10 rounded-2xl bg-sage p-6"><div className="flex justify-between text-sm font-bold"><span>Comfortable monthly budget</span><span className="text-moss">${budget.toLocaleString()}</span></div><input aria-label="Monthly budget" type="range" min="2000" max="12000" step="250" value={budget} onChange={e => setBudget(Number(e.target.value))} className="mt-6 w-full accent-[#527060]" /><div className="mt-3 flex justify-between text-xs text-slate-500"><span>$2,000</span><span>$12,000+</span></div></div></div><div className="rounded-3xl bg-[#f3eee5] p-8 md:p-10"><DollarSign className="text-amber" size={30} /><p className="mt-7 text-sm font-bold text-slate-500">Your starting range</p><h2 className="mt-2 font-serif text-5xl">${item.min.toLocaleString()}–${item.max.toLocaleString()}</h2><p className="mt-4 text-sm leading-6 text-slate-600">Typical monthly range for {item.name.toLowerCase()} in the Raleigh-Durham area. Actual pricing depends on care needs, floor plan, and community.</p><div className="mt-8 border-t border-slate-300/60 pt-6"><p className="text-sm font-bold">Ways families often pay</p><div className="mt-4 grid gap-3 text-sm text-slate-600"><span className="flex gap-2"><Check className="text-moss" size={17} /> Private pay or family resources</span><span className="flex gap-2"><Check className="text-moss" size={17} /> VA Aid & Attendance benefits</span><span className="flex gap-2"><Check className="text-moss" size={17} /> Long-term care insurance</span></div></div><a href="/assessment" className="mt-8 flex items-center justify-center gap-2 rounded-full bg-ink px-5 py-4 font-bold text-white">Build my care plan <ArrowRight size={17} /></a></div></section></> }
