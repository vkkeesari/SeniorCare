'use client';
import Link from 'next/link';
import { ArrowRight, Menu, Phone, X } from 'lucide-react';
import { useState } from 'react';
export default function Header() { const [open, setOpen] = useState(false); const links = [['Care options','/care-options'],['Care costs','/care-costs'],['Our approach','/about']]; return <header className="sticky top-0 z-40 border-b border-slate-200/80 bg-[#fafafa]/90 backdrop-blur-md"><div className="container-shell flex h-[76px] items-center justify-between"><Link href="/" className="flex items-center gap-2"><span className="grid h-10 w-10 place-items-center rounded-xl bg-ink font-serif text-xl text-white">G</span><span className="text-[15px] font-extrabold tracking-tight">Gabe's <span className="font-normal text-moss">Senior Care</span></span></Link><nav className="hidden items-center gap-8 md:flex">{links.map(([label, href]) => <Link key={href} href={href} className="text-sm font-semibold text-slate-600 transition hover:text-ink">{label}</Link>)}<a href="tel:9195550148" className="flex items-center gap-2 text-sm font-bold"><Phone size={15} /> (919) 555-0148</a><Link href="/assessment" className="flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-sm font-bold text-white transition hover:bg-moss">Quick assessment <ArrowRight size={15} /></Link></nav><button aria-label="Toggle navigation" onClick={() => setOpen(!open)} className="md:hidden">{open ? <X /> : <Menu />}</button></div>{open && <nav className="border-t border-slate-200 bg-cream px-5 py-5 md:hidden">{links.map(([label, href]) => <Link onClick={() => setOpen(false)} key={href} href={href} className="block border-b border-slate-200 py-4 font-semibold">{label}</Link>)}<Link href="/assessment" onClick={() => setOpen(false)} className="mt-4 block rounded-full bg-ink px-5 py-3 text-center font-bold text-white">Start quick assessment</Link></nav>}</header> }
